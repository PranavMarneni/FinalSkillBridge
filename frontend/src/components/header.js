import React from 'react';
import './header.css'; // Add a separate CSS file for styling if needed


const Header = () => {
  return (
    <header className="header">
      <h1>SkillBridge</h1>
    </header>
  );
};

export default Header;
